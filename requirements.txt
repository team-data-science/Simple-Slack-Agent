slack_sdk[socket_mode]   # Slack SDK with Socket Mode support for real-time event handling
langchain>=0.3.0         # New LangChain version (includes create_agent)
langchain-core>=0.3.0    # Core abstractions (messages, runnables, etc.)
langchain-ollama>=0.2.0  # Updated Ollama integration compatible with LangChain 0.3+
python-dotenv            # Loads environment variables from .env file
requests                 # HTTP client for calling external APIs (e.g. Databricks)








